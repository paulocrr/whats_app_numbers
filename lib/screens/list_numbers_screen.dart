import 'package:flutter/material.dart';

class ListNumberScreen extends StatelessWidget {
  const ListNumberScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
